// const db = require("../db");

// const createDbApiRoutes = app => {
//     app.get("/api/notes", (req, res) =>
//         db.readNote().then(notes => res.json(notes))
//     );
// };


// module.exports = createDbApiRoutes;
